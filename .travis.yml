sudo: false
language: node_js
node_js:
  - "8.0"
  - "node"
env:
  - CXX=g++-5

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
  hosts:
    - nic.localhost
    - tim.localhost
    - nicola.localhost

cache:
  apt: true
  directories:
    - node_modules
